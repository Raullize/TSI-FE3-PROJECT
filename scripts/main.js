// Arquivo principal para interações do usuário

document.addEventListener('DOMContentLoaded', () => {
    console.log('Página inicial carregada.');
    // Lógica futura para a página inicial pode ser adicionada aqui.
});